# vso-vmware - will be used to develope the tasks and extensions for vmware

# To build Java code

    # Ensure you have instlled the maven
    # compile - mvn compile
    # run test  -   mvn test
    # code coverage -   mvn verify
            Go to _build _build\codecoverage folder to check the code coverage